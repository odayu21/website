"use strict";(self.webpackChunkthree_shader_env01=self.webpackChunkthree_shader_env01||[]).push([[185],{185(e,n,t){t.r(n),t.d(n,{default:()=>l});var i=t(437),o=t(566);const r=250,a=300*Math.random(),s=[{PLT1:new i.Pq0(.962898,.249135,.751898),PLT2:new i.Pq0(.925892,.966695,.995909),PLT3:new i.Pq0(.1,.74,.72),PLT4:new i.Pq0(.2,.91,.78)},{PLT1:new i.Pq0(.2,.5,.8),PLT2:new i.Pq0(.2,.7,.5),PLT3:new i.Pq0(.1,.74,.72),PLT4:new i.Pq0(.2,.91,.78)}],c=a<150?1:0;class l{constructor(e){this.canvas=e,this.isRunning=!1,this.sizes={width:window.innerWidth,height:window.innerHeight},this.activePaletteIndex=c,this.init()}init(){this.timeOffset=0,this.timeOffsetTarget=0,this.timeTransitionDuration=2.5,this.TIME_JUMP=10,this.transitionStart=null,this._lastS=0,this.pltFrom=[new i.Pq0,new i.Pq0,new i.Pq0,new i.Pq0],this.pltTo=[new i.Pq0,new i.Pq0,new i.Pq0,new i.Pq0],this.initScene(),this.initInstances(),this.initRenderer(),this.initClickHandler(),this.clock=new i.zD7,this.start()}initScene(){this.scene=new i.Z58,this.camera=new i.ubm(45,this.sizes.width/this.sizes.height,.3,100),this.camera.position.set(0,0,10),this.camera.lookAt(0,0,0),this.scene.add(this.camera);const e=new Float32Array(48);this.geometry=new i.LoY,this.geometry.setAttribute("position",new i.THS(e,3));const n=s[c];this.material=new i.BKk({vertexShader:"smooth out vec4  color;\nflat out int   isCircle;\nsmooth out vec2  corner;\nsmooth out float texCorrector;\nsmooth out float depth;\n\n// in vec3 position;\nin vec2 instanceUV;\n\nuniform vec3 uPLT1, uPLT2, uPLT3, uPLT4;\nuniform float camDist, uTime, camW, zoom;\nuniform int dim;\n\n#define W_SCALE2 2.5\n#define WIDTH 0.012\n#define APT_BLUR 230.\n#define FAR_CLIP 100.\n#define NEAR_CLIP 0.3\n\n//p noise helper\nvec3 mod289(vec3 x) {\n    return x - floor(x * (1.0 / 289.0)) * 289.0;\n}\n\nvec4 mod289_4(vec4 x) {\n    return x - floor(x * (1.0 / 289.0)) * 289.0;\n}\n\nvec4 permute(vec4 x) {\n    return mod((x * 34.0 + 1.0) * x, 289.0);\n}\n\nvec4 taylorInvSqrt(vec4 r) {\n    return 1.79284291400159 - 0.85373472095314 * r;\n}\n\nvec3 fade(vec3 t) {\n    return t * t * t * (t * (t * 6.0 - 15.0) + 10.0);\n}\n\nvec4 fade_4(vec4 t) {\n    return t * t * t * (t * (t * 6.0 - 15.0) + 10.0);\n}\n\nvec4 pnoise4(vec2 p, float time) {\n    const vec3 rep = vec3(89.42, 122.37, 201.53);\n    const float inv7 = 1.0 / 7.0;\n    \n    vec4 result;\n\n    for(int i = 0; i < 4; i++) {\n        float t = time + float(i);\n        vec3 P = vec3(p, t);\n\n        vec3 Pi0 = mod(floor(P), rep);\n        vec3 Pi1 = mod(Pi0 + 1.0, rep);\n        Pi0 = mod289(Pi0);\n        Pi1 = mod289(Pi1);\n\n        vec3 Pf0 = fract(P);\n        vec3 Pf1 = Pf0 - 1.0;\n\n        vec4 ix = vec4(Pi0.x, Pi1.x, Pi0.x, Pi1.x);\n        vec4 iy = vec4(Pi0.yy, Pi1.yy);\n\n        vec4 ixy = permute(permute(ix) + iy);\n        vec4 ixy0 = permute(ixy + Pi0.zzzz);\n        vec4 ixy1 = permute(ixy + Pi1.zzzz);\n\n        vec4 gx0 = fract(ixy0 * inv7);\n        vec4 gx1 = fract(ixy1 * inv7);\n\n        vec4 gy0 = fract(floor(ixy0 * inv7) * inv7) - 0.5;\n        vec4 gy1 = fract(floor(ixy1 * inv7) * inv7) - 0.5;\n\n        vec4 gz0 = 0.5 - abs(gx0) - abs(gy0);\n        vec4 gz1 = 0.5 - abs(gx1) - abs(gy1);\n\n        vec4 sz0 = step(gz0, vec4(0.0));\n        vec4 sz1 = step(gz1, vec4(0.0));\n\n        gx0 -= sz0 * (step(0.0, gx0) - 0.5);\n        gy0 -= sz0 * (step(0.0, gy0) - 0.5);\n        gx1 -= sz1 * (step(0.0, gx1) - 0.5);\n        gy1 -= sz1 * (step(0.0, gy1) - 0.5);\n\n        // Compact normalization\n        vec4 norm0 = taylorInvSqrt(gx0 * gx0 + gy0 * gy0 + gz0 * gz0);\n        vec4 norm1 = taylorInvSqrt(gx1 * gx1 + gy1 * gy1 + gz1 * gz1);\n\n        gx0 *= norm0;\n        gy0 *= norm0;\n        gz0 *= norm0;\n        \n        gx1 *= norm1;\n        gy1 *= norm1;\n        gz1 *= norm1;\n\n        // Explicit position offsets\n        vec3 p000 = Pf0;\n        vec3 p100 = vec3(Pf1.x, Pf0.yz);\n        vec3 p010 = vec3(Pf0.x, Pf1.y, Pf0.z);\n        vec3 p110 = vec3(Pf1.xy, Pf0.z);\n        vec3 p001 = vec3(Pf0.xy, Pf1.z);\n        vec3 p101 = vec3(Pf1.x, Pf0.y, Pf1.z);\n        vec3 p011 = vec3(Pf0.x, Pf1.yz);\n        vec3 p111 = Pf1;\n\n        // Dot products\n        float n000 = dot(vec3(gx0.x, gy0.x, gz0.x), p000);\n        float n100 = dot(vec3(gx0.y, gy0.y, gz0.y), p100);\n        float n010 = dot(vec3(gx0.z, gy0.z, gz0.z), p010);\n        float n110 = dot(vec3(gx0.w, gy0.w, gz0.w), p110);\n\n        float n001 = dot(vec3(gx1.x, gy1.x, gz1.x), p001);\n        float n101 = dot(vec3(gx1.y, gy1.y, gz1.y), p101);\n        float n011 = dot(vec3(gx1.z, gy1.z, gz1.z), p011);\n        float n111 = dot(vec3(gx1.w, gy1.w, gz1.w), p111);\n\n        vec3 f = fade(Pf0);\n\n        vec4 n_z = mix(\n            vec4(n000, n100, n010, n110),\n            vec4(n001, n101, n011, n111),\n            f.z\n        );\n\n        vec2 n_yz = mix(n_z.xy, n_z.zw, f.y);\n\n        result[i] = 2.2 * mix(n_yz.x, n_yz.y, f.x);\n    }\n\n    return result;\n}\n// --- p noise helper end\n\n\nvec3 palette(float t) {\n    return uPLT1 + uPLT2 * cos(6.28318 * (uPLT3 * t + uPLT4));\n}\n\n#if 1\nvec3 SGP(vec4 point) {\n    float denominator = W_SCALE2 / max(W_SCALE2 - point.w, 0.001);\n    return vec3(point.xyz * denominator);\n}\n#else\nvec3 SGP(vec4 point) {\n    return vec3(point.xyz);\n}\n#endif\n\nfloat nFold(float x) {\n    float m = abs(mod(x, 1.));\n    return (mod(floor(x), 2.0) == 0.0) ? m : 1. - m;\n}\n\nconst vec4 TEMPLATE[16] = vec4[16](\n    // part 0 — main quad\n    vec4(-1,  0, 0, 0),  //  0: left0\n    vec4(-1,  0, 0, 0),  //  1: left1\n    vec4( 1,  0, 0, 0),  //  2: right0\n    vec4( 1,  0, 0, 0),  //  3: right1\n    // degenerate break\n    vec4( 0,  0, 0, 4),  //  4\n    vec4( 0,  0, 0, 4),  //  5\n    // part 2 — cap 0\n    vec4(-1,  1, 1, 2),  //  6: left0  + capOffset0\n    vec4(-1,  0, 1, 2),  //  7: left0\n    vec4( 1,  1, 1, 2),  //  8: right0 + capOffset0\n    vec4( 1,  0, 1, 2),  //  9: right0\n    // degenerate break\n    vec4( 0,  0, 0, 4),  // 10\n    vec4( 0,  0, 0, 4),  // 11\n    // part 3 — cap 1\n    vec4(-1,  0, 1, 3),  // 12: left1\n    vec4(-1, -1, 1, 3),  // 13: left1  + capOffset1\n    vec4( 1,  0, 1, 3),  // 14: right1\n    vec4( 1, -1, 1, 3)   // 15: right1 + capOffset1\n);\n\nvec2 rotate(vec2 point, float angle) {\n    float c = cos(angle), s = sin(angle);\n    return vec2(point.x * c - point.y * s,\n                point.x * s + point.y * c);\n}\n\nvoid main() {\n    // const float width = 0.0145;\n    const float width = WIDTH;\n\n    vec4 t        = TEMPLATE[gl_VertexID];\n    int  partIsCircle = int(t.z);\n    int  part     = int(t.w);\n\n    ivec2 tc0 = ivec2(int(instanceUV.x + 0.5) * 2,     int(instanceUV.y + 0.5));\n    ivec2 tc1 = ivec2(int(instanceUV.x + 0.5) * 2 + 1, int(instanceUV.y + 0.5));\n    vec2 texSize = vec2(float(dim) * 2., float(dim));\n        // vec2 texSize = vec2(400, 200);\n    vec2 uv0 = (vec2(tc0) + vec2(0.5)) / texSize;\n    vec2 uv1 = (vec2(tc1) + vec2(0.5)) / texSize;\n    // THIS IS IT!!\n    vec2 offset = vec2(zoom * 2., zoom);\n    vec2 p0 = uv0 * offset + offset * -0.5; \n    vec2 p1 = uv1 * offset + offset * -0.5;\n\n    vec4 raw0 = pnoise4(p0, uTime * 0.03) * 10.;\n    vec4 raw1 = pnoise4(p1, uTime * 0.03) * 10.;\n\n\n    float camPosW = camW * 0.8;\n\n    vec4 view0 = vec4((viewMatrix * vec4(raw0.xyz, 1.)).xyz, raw0.w - camPosW);\n    vec4 view1 = vec4((viewMatrix * vec4(raw1.xyz, 1.)).xyz, raw1.w - camPosW);\n\n    // SGP projection — keeping xy from SGP, z from view space (matching original behaviour)\n    vec4 new0 = vec4(SGP(view0).xy, view0.z, 1.);\n    vec4 new1 = vec4(SGP(view1).xy, view1.z, 1.);\n    // vec4 new0 = vec4(SGP(view0), 1.);\n    // vec4 new1 = vec4(SGP(view1), 1.);\n\n    // Color per endpoint\n    float w0 = raw0.w * 2. - camPosW * 0.5;\n    float w1 = raw1.w * 2. - camPosW * 0.5;\n\n    vec3  col0   = palette(w0 - uTime * 0.2);\n    vec3  col1   = palette(w1 - uTime * 0.2);\n    float alpha0 = mix(0.2, 1., nFold(uTime * 0.15 + w0 * 0.92));\n    float alpha1 = mix(0.2, 1., nFold(uTime * 0.15 + w1 * 0.92));\n\n    vec4 color0 = vec4(col0, alpha0);\n    vec4 color1 = vec4(col1, alpha1);\n\n    // Depth / aperture blur — use xyz only to match Stage 1's length(viewPos)\n    float focus  = (camDist - NEAR_CLIP) / (FAR_CLIP - NEAR_CLIP);\n    float depth0 = (length(new0.xyz) - NEAR_CLIP) / (FAR_CLIP - NEAR_CLIP);\n    float depth1 = (length(new1.xyz) - NEAR_CLIP) / (FAR_CLIP - NEAR_CLIP);\n    depth0 = min(1., 1. / (abs(depth0 - focus) * APT_BLUR + 1.));\n    depth1 = min(1., 1. / (abs(depth1 - focus) * APT_BLUR + 1.));\n\n    // Line geometry — perspective divide xy/z before computing angle, matching Option 2\n    // float width = uLInfo.z;\n    \n\n    vec2 proj0 = new0.xy / new0.z;\n    vec2 proj1 = new1.xy / new1.z;\n    float angle = atan(proj0.x - proj1.x, proj1.y - proj0.y);\n\n    vec2 offsetLeft  = rotate(vec2(-width, 0.), angle);\n    vec2 offsetRight = rotate(vec2( width, 0.), angle);\n\n    vec4 vertLeft0  = new0 + vec4(offsetLeft  / depth0, 0., 0.);\n    vec4 vertRight0 = new0 + vec4(offsetRight / depth0, 0., 0.);\n    vec4 vertLeft1  = new1 + vec4(offsetLeft  / depth1, 0., 0.);\n    vec4 vertRight1 = new1 + vec4(offsetRight / depth1, 0., 0.);\n\n    vec4 capOffset0 = vec4(rotate(vec2(0.,  width), angle), 0., 0.);\n    vec4 capOffset1 = vec4(rotate(vec2(0., -width), angle), 0., 0.);\n\n    vec4  finalPos     = vec4(0.);\n    vec4  finalColor   = vec4(0.);\n    float finalDepth   = 0.;\n    vec2  finalCorner  = vec2(0.);\n    float finalTexCorr = 1.;\n\n    if (part == 4) {\n        if      (gl_VertexID == 4)  finalPos = vertRight1;\n        else if (gl_VertexID == 5)  finalPos = vertLeft0 + capOffset0 / depth0;\n        else if (gl_VertexID == 10) finalPos = vertRight0;\n        else if (gl_VertexID == 11) finalPos = vertLeft1;\n\n        gl_Position          = projectionMatrix * finalPos;\n        color        = vec4(0.);\n        isCircle     = 0;\n        corner       = vec2(0.);\n        texCorrector = 1.;\n        depth        = 0.;\n        return;\n    }\n\n    float length0 = length(vertLeft0 - vertRight0);  // move these BEFORE the if/else chain\n    float length1 = length(vertLeft1 - vertRight1);\n\n    // ...\n    if (part == 0) {\n        if      (gl_VertexID == 0) { finalPos = vertLeft0;  finalDepth = depth0; finalCorner = vec2(-length0, 0.); finalTexCorr = length0; finalColor = vec4(color0.rgb, color0.a * depth0); }\n        else if (gl_VertexID == 1) { finalPos = vertLeft1;  finalDepth = depth1; finalCorner = vec2(-length1, 0.); finalTexCorr = length1; finalColor = vec4(color1.rgb, color1.a * depth1); }\n        else if (gl_VertexID == 2) { finalPos = vertRight0; finalDepth = depth0; finalCorner = vec2( length0, 0.); finalTexCorr = length0; finalColor = vec4(color0.rgb, color0.a * depth0); }\n        else if (gl_VertexID == 3) { finalPos = vertRight1; finalDepth = depth1; finalCorner = vec2( length1, 0.); finalTexCorr = length1; finalColor = vec4(color1.rgb, color1.a * depth1); }\n    }\n\n    else if (part == 2) {\n        if      (gl_VertexID == 6)  { finalPos = vertLeft0  + capOffset0 / depth0; finalDepth = depth0; finalCorner = vec2(-1.,  1.); finalColor = vec4(color0.rgb, color0.a * depth0); }\n        else if (gl_VertexID == 7)  { finalPos = vertLeft0;                        finalDepth = depth0; finalCorner = vec2(-1.,  0.); finalColor = vec4(color0.rgb, color0.a * depth0); }\n        else if (gl_VertexID == 8)  { finalPos = vertRight0 + capOffset0 / depth0; finalDepth = depth0; finalCorner = vec2( 1.,  1.); finalColor = vec4(color0.rgb, color0.a * depth0); }\n        else if (gl_VertexID == 9)  { finalPos = vertRight0;                       finalDepth = depth0; finalCorner = vec2( 1.,  0.); finalColor = vec4(color0.rgb, color0.a * depth0); }\n    }\n\n    else if (part == 3) {\n        if      (gl_VertexID == 12) { finalPos = vertLeft1;                        finalDepth = depth1; finalCorner = vec2(-1.,  0.); finalColor = vec4(color1.rgb, color1.a * depth1); }\n        else if (gl_VertexID == 13) { finalPos = vertLeft1  + capOffset1 / depth1; finalDepth = depth1; finalCorner = vec2(-1., -1.); finalColor = vec4(color1.rgb, color1.a * depth1); }\n        else if (gl_VertexID == 14) { finalPos = vertRight1;                       finalDepth = depth1; finalCorner = vec2( 1.,  0.); finalColor = vec4(color1.rgb, color1.a * depth1); }\n        else if (gl_VertexID == 15) { finalPos = vertRight1 + capOffset1 / depth1; finalDepth = depth1; finalCorner = vec2( 1., -1.); finalColor = vec4(color1.rgb, color1.a * depth1); }\n    }\n\n    finalColor.g *= 0.6;\n    gl_Position     = projectionMatrix * finalPos;\n    color           = finalColor;\n    isCircle        = partIsCircle;\n    corner          = finalCorner;\n    texCorrector    = finalTexCorr;\n    depth           = finalDepth;\n}\n  ",fragmentShader:"#define FADE 0.0\n\nsmooth in vec4  color;\nflat in int   isCircle;\nsmooth in vec2  corner;\nsmooth in float texCorrector;\nsmooth in float depth;\n\nvoid main() {\n\n    float fade = FADE;\n\n    if(isCircle == 1){\n        float dist = length(corner);\n        if (dist > 1.) discard;\n        dist = smoothstep(fade, 1., dist);\n        gl_FragColor = color;\n        gl_FragColor.a *= (1.0 - dist) * depth;\n\n    } else {\n        gl_FragColor = color;\n        float dist = abs(corner.x/texCorrector);\n        dist = smoothstep(fade, 1., dist);\n        gl_FragColor.a *= (1.0 - dist) * depth;\n    }\n    // gl_FragColor.a *= 0.8;\n    // gl_FragColor.a *= gl_FragColor.a * gl_FragColor.a;\n    // gl_FragColor.a *= pow(gl_FragColor.a, 1.);\n    float alphaCut = 0.0005;\n    if (color.a < alphaCut) discard;\n\n}",transparent:!0,depthTest:!0,depthWrite:!1,blending:i.NTi,uniforms:{uTime:{value:0},camDist:{value:0},camW:{value:0},zoom:{value:1.5},dim:{value:r},uPLT1:{value:n.PLT1.clone()},uPLT2:{value:n.PLT2.clone()},uPLT3:{value:n.PLT3.clone()},uPLT4:{value:n.PLT4.clone()}}})}initInstances(){this.instanceCount=62500;const e=new Float32Array(2*this.instanceCount);let n=0;for(let t=0;t<r;t++)for(let i=0;i<r;i++)e[n++]=i,e[n++]=t;this.geometry.setAttribute("instanceUV",new i.uWO(e,2)),this.geometry=(0,o._c)(this.geometry,i.O49),this.mesh=new i.ZLX(this.geometry,this.material,this.instanceCount),this.scene.add(this.mesh)}initRenderer(){this.renderer=new i.JeP({canvas:this.canvas}),this.renderer.setClearColor(0,1),this.resize()}initClickHandler(){this._onClick=()=>{const e=this.clock.getElapsedTime();this.timeOffsetTarget=this.timeOffset+this.TIME_JUMP,this.activePaletteIndex=(this.activePaletteIndex+1)%s.length;const n=s[this.activePaletteIndex];this.pltFrom[0].copy(this.material.uniforms.uPLT1.value),this.pltFrom[1].copy(this.material.uniforms.uPLT2.value),this.pltFrom[2].copy(this.material.uniforms.uPLT3.value),this.pltFrom[3].copy(this.material.uniforms.uPLT4.value),this.pltTo[0].copy(n.PLT1),this.pltTo[1].copy(n.PLT2),this.pltTo[2].copy(n.PLT3),this.pltTo[3].copy(n.PLT4),this.transitionStart=e,this._lastS=0},window.addEventListener("click",this._onClick)}updateTransitions(e){if(null===this.transitionStart)return;const n=e-this.transitionStart,t=Math.min(n/this.timeTransitionDuration,1),i=t*t*(3-2*t);this.timeOffset+=(this.timeOffsetTarget-this.timeOffset)*(i-this._lastS)/Math.max(1-this._lastS,1e-4),this._lastS=i;const o=[this.material.uniforms.uPLT1,this.material.uniforms.uPLT2,this.material.uniforms.uPLT3,this.material.uniforms.uPLT4];for(let e=0;e<4;e++)o[e].value.lerpVectors(this.pltFrom[e],this.pltTo[e],i);t>=1&&(this.timeOffset=this.timeOffsetTarget,this.transitionStart=null,this._lastS=0)}animate(){if(!this.isRunning)return;const e=this.clock.getElapsedTime();this.updateTransitions(e);const n=e+this.timeOffset,t=.7*Math.sin(.5*n),i=.9+.5*(Math.sin(.15*n-Math.PI/2)+1)*1,o=this.camera.position.x,r=this.camera.position.y,s=this.camera.position.z,c=Math.sqrt(o*o+r*r+s*s+t*t);this.material.uniforms.uTime.value=n+a,this.material.uniforms.camW.value=t,this.material.uniforms.zoom.value=i,this.material.uniforms.camDist.value=c,this.renderer.render(this.scene,this.camera),requestAnimationFrame(()=>this.animate())}resize(){this.sizes.width=window.innerWidth,this.sizes.height=window.innerHeight,this.camera.aspect=this.sizes.width/this.sizes.height,this.camera.updateProjectionMatrix(),this.renderer.setSize(this.sizes.width,this.sizes.height),this.renderer.setPixelRatio(Math.min(window.devicePixelRatio,2))}start(){this.isRunning||(this.isRunning=!0,this.clock.start(),this.animate())}stop(){this.isRunning=!1,this.clock.stop()}refresh(){}destroy(){this.stop(),window.removeEventListener("click",this._onClick),this.renderer.dispose()}}}}]);